# Legacy

`index.html` is the original single-file block-letter tool — the one that made
the page *icons*, not the covers. It has no build step; open it directly.

The cover generator it used to link to has been replaced by the app at the
repo root (`npm run dev`).
